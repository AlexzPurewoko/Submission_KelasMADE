<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="id.apwdevs.app.catalogue">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="id.apwdevs.app.catalogue.READ_DATABASE"/>
    <uses-permission android:name="id.apwdevs.app.catalogue.WRITE_DATABASE"/>

    <permission
            android:name="id.apwdevs.app.catalogue.READ_DATABASE"
            android:protectionLevel="normal"/>
    <permission
            android:name="id.apwdevs.app.catalogue.WRITE_DATABASE"
            android:protectionLevel="normal"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <receiver
                android:name=".receiver.NotifyBootCompleted"
                android:enabled="true"
                android:exported="true">
            <intent-filter>
                <action
                        android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <receiver
                android:name=".receiver.StartExactJobReceiver"
                android:enabled="true"
                android:exported="true">
        </receiver>
        <receiver
                android:name=".receiver.LaunchReceiver"
                android:enabled="true"
                android:exported="true">
            <intent-filter>
                <action android:name="id.apwdevs.app.catalogue.LAUNCH_MAIN_ACTIVITY"/>
                <action android:name="id.apwdevs.app.catalogue.LAUNCH_DETAIL_ACTIVITY"/>
            </intent-filter>
        </receiver>

        <activity
                android:name=".activities.SettingsActivity"
                android:label="Settings"
                android:noHistory="true"
                android:parentActivityName=".activities.MainTabUserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>

            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="id.apwdevs.app.catalogue.activities.MainTabUserActivity"/>
        </activity>
        <activity
                android:name=".activities.DetailActivity"
                android:label="@string/title_activity_detail"
                android:parentActivityName=".activities.MainTabUserActivity"
                android:theme="@style/AppTheme.NoActionBar.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>

            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="id.apwdevs.app.catalogue.activities.MainTabUserActivity"/>
        </activity>
        <activity
                android:name=".activities.MainTabUserActivity"
                android:launchMode="singleTask"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <provider
                android:name=".provider.FavoriteProvider"
                android:authorities="id.apwdevs.app.catalogue"
                android:exported="true"
                android:readPermission="id.apwdevs.app.catalogue.READ_DATABASE"
                android:writePermission="id.apwdevs.app.catalogue.WRITE_DATABASE"/>
    </application>

</manifest>